{
    "require": {
        "tightenco/jigsaw": "^1.3",
        "tightenco/jigsaw-blog-template": "^1.2",
        "samdark/sitemap": "^2.2.1"
    },
    "autoload": {
        "psr-4": {
            "App\\Listeners\\": "listeners/"
        }
    },
    "scripts": {
        "build:prod": [
            "bun run build:assets",
            "jigsaw build production"
        ],
        "build": [
            "bun run build:assets",
            "jigsaw build local"
        ]
    },
    "scripts-descriptions": {
        "build:prod": "Compile the frontend assets and build the production site into build_production/.",
        "build": "Compile the frontend assets and build the local site into build_local/."
    }
}