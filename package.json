{
    "private": true,
    "scripts": {
        "build": "bun run build:assets && vendor/bin/jigsaw build production",
        "build:assets": "vite build",
        "dev": "bun run build:assets && vendor/bin/jigsaw build local",
        "watch": "bun run build:assets && concurrently \"vite build --watch\" \"vendor/bin/jigsaw build local --watch\"",
        "prod": "vite build",
        "production": "vite build"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.3.3",
        "concurrently": "^10.0.5",
        "fuse.js": "^7.5.0",
        "highlight.js": "^11.12.0",
        "tailwindcss": "^4.3.3",
        "vite": "^6.4.3"
    }
}
